language: node_js
node_js:
  - 6.1.0

before_install:
  - export CHROME_BIN=chromium-browser
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

install:
  - npm install

cache:
  directories:
    - node_modules

branches:
  only:
    - master
